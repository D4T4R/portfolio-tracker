flask==2.3.3
yahooquery==2.3.7
flask-cors==4.0.0
sqlalchemy==2.0.21
# The ledger runs on Postgres once hosted and on SQLite in tests, so the
# driver is only needed where DATABASE_URL points at Postgres. Listed anyway:
# app.py mounts the v2 API on nothing but that variable, and discovering the
# missing driver at import time on the host is a worse place to learn it.
psycopg2-binary==2.9.9
# Flask's built-in server is single-threaded and says itself it is not for
# production. Every price sync holds a worker for the length of a Yahoo call.
gunicorn==21.2.0
pandas==2.0.3
# pandas 2.0.3 ships wheels built against the numpy 1.x C ABI; an unpinned
# numpy resolves to 2.x and pandas then fails to import with
# "numpy.dtype size changed, may indicate binary incompatibility".
numpy<2
openpyxl==3.1.2